global:
  message: Welcome to the micoroservice project
logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include : *

  endpoint: 
    health:
      show-details: always


# eureka:
#     client:
#         register-with-eureka: false
#         fetch-registry: false
#         service-url:
#             defaultZone: http://localhost:8761/eureka/
