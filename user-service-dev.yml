gloabl:
  message: welcome to the user service dev env

server:
  port: 8082

message: hello from dev!!

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}

info:
  app:
    name: userservice
    description: User Management Microservice dev
    version: 1.0.0
  company:
    name: MyCompany
    website: https://mycompany.com
