gloabl:
  message: welcome to the order service pro env

server:
  port: 8085

message: hello from productions!!

management:
  endpoints:
    web:
      exposure:
        include: health, info, env, metrics
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
